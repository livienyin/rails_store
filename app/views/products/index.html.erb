<h1>Listing products</h1>
<%= link_to 'New Product', new_product_path %>
<%= link_to 'Product Reviews', reviews_path %>
<table>
  <tr>
    <th>Image</th>
    <th>Name</th>
    <th>Price</th>
    <th></th>
    <th></th>
  </tr>

<% @products.each do |product| %>
  <tr>
    <td><img src="<%= product.image_url %>" height="50"></td>
    <td><%= product.name %></td>
    <td><%= product.price %></td>
    <td><%= link_to 'Add To Cart' %> <!--FILL -->
    <td><%= link_to 'Show', product %></td>
    <td><%= link_to 'Edit', edit_product_path(product) %></td>
    <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%#= render 'form.html' %>
<%= render 'sidecar.html' %>


